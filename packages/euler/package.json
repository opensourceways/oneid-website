{
  "name": "euler",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build"
  },
  "dependencies": {
    "shared": "workspace:^",
    "unplugin-icons": "^0.14.0"
  }
}